# Local MongoDB for development and tests: docker compose up -d
services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - student-mongo-data:/data/db

volumes:
  student-mongo-data:
