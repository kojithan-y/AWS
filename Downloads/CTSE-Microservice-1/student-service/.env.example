# Copy to .env. Prefer host-only URI + separate credentials so @ : # in passwords are encoded correctly.
MONGODB_URI=mongodb+srv://YOUR_CLUSTER.mongodb.net/student-service?retryWrites=true&w=majority&appName=Cluster0
MONGODB_USER=
MONGODB_PASSWORD=

PORT=3000
